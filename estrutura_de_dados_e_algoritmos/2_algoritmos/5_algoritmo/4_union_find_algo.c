#include <stdio.h>

/**
 * Algoritmo de Union-Find: O algoritmo Union-Find, também conhecido como algoritmo de
 * "conjunto disjunto" ou "Disjoint Set Union" (DSU), uma estrutura de dados utilizada
 * para gerenciar e combinar conjuntos disjuntos de elementos armazenando arvores
 * disjuntas e permite que sejam unidas duas arvores.
 * Bastante útil para resolver problemas que envolvem união e busca de elementos
 * em subconjuntos, como em grafos e redes.
 *
 * COMPLEXIDADE:
 * TEMPO: O(n)
 *
 * ESPAÇO: O(1)
 */