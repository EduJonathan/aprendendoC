/** Em processo... */
#include <stdio.h>
#include <stddef.h>

/**
 * A biblioteca stddef, nos ajuda em operadores lógicos e bitwises que ao vez de declaramos
 * com seus respectivos símbolos, podemos simplesmente utilizar a nomeação definida.
 * Seu uso é OPCIONAL.
 */

int main(int argc, char **argv)
{
    off32_t _32; // 32 bits
    off64_t _64; // 64 bits
    off_t l;     // 32 ou 64 bits dependendo do S.O
    return 0;
}
