#include <stdio.h>

// EOF (End Of File): macro definida na biblioteca padrão de C (<stdio.h>). Essa constante
// é usada para indicar o fim de um arquivo quando a leitura ou escrita atinge o final.
// O valor de EOF é geralmente -1.